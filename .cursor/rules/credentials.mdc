# Credenciales

Todas las credenciales (tokens, .env) viven en **resources/secrets/**.

- Usar el `.env` en ese directorio; no hardcodear rutas ni claves en el código.
- Los scripts del proyecto cargan variables con `scripts.credentials.load_env()`.
